# Kimpo and Hanam observed empty archives

Supervisor/designer: actual Astra. Goal: recognize real GOPA and Hanam final responses, without skipping populated or unknown pages. Independent ownership: ONLY new src/collect/board/municipal-page-end.ts and municipal-page-end.test.ts. Export isProvenMunicipalEnd(html: string, cfg: BoardConfig, requestedPage?: number): boolean. Root connects after global auth/error guard, retains consecutive-two-page rule, and runs tests/review/deploy. No changes to page-end.ts, existing parsers, normal collection, stores, schema, UI or network.

GOPA captured 2026-09-13 pages18/19: exactly one div.list.list2, its .tot strong span says100; one direct ul contains exactly one li with compact text 검색된내용이없습니다. No announcement links or unknown extra element/text rows. Fixed six rows per page, ceil(total/6)=17, requestedPage must exceed17. Only cfg.id gopa. No inference from page budget or repeated rows. Preserve absent/current-page proof as incomplete. The caller binds requested page to actual request.

Hanam captured pages18/19: standard table.p-table.simple absent. Exactly one div.p-empty.nofile > .p-empty_box > .p-empty_text with compact text 등록된게시물이없습니다. (span.p-empty_word inside is legitimate; reject anchors/other populated siblings or duplicate empty containers.) .p-page .count has exactly two em.em_count texts162 and18/17 or19/17. Fixed10perpage, positive safe integer total/current/last; current===requestedPage>last===ceil(total/10). Require canonical source-board pagination under one .p-pagination, same https origin /biz/selectBbsNttList.do, key6003, bbsNo1632, unique pageIndex within1..last; require observed first link page1 and last-page17 link. Other observed query keys are searchCtgry(empty), pageUnit10, searchCnd=all, searchKrwd(empty), integrDeptCode(empty), selectPageUnit(empty). Reject foreign paths/origins, view actions, duplicate query keys, contradictory pageUnit, nonempty filters. Actual page19 prev-one points18 beyondlast: do not accept prev-one as proof, verify numeric link-group plus first link only; ignore observed prev navigation out-of-range, reject unknown navigation shape as incomplete. Keep proof narrow, don't overfit current total162.

Tests: current observed positive shapes18/19; sources mismatch; missing/duplicate/contradictory metadata; current page mismatch; zero/negative/decimal totals; real lastpage not end; populated/undated rows; unknown content; foreign/ambiguous query; no proof from footer text alone. Root tests through fetchBoardWindow/global error guard separately. Build synthetic minimal fixtures from observed structure, no network calls. Root preserved public capture files privately and will run final actual proofs. Worker must not shell/network/commit/deploy, root handles gates.
