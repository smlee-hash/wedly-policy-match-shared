# GTP archived policy dates from official detail pages

Goal: historical GTP pages must retain actual closed support announcements. Current parser intentionally omits rows whose period cell says 마감 to avoid reopening them without a real end date. Root verified page5 has10suchpolicyrows, all10official detail pages contain real start/end dates. Observed172316=2026-08-07~2026-08-31;172315=2026-08-21~2026-09-11;172313=2026-08-19~2026-08-31. Never manufacture dates or change normal newest-board behavior.

Implementation ownership: src/collect/board/gtp-archive.ts and test; src/collect/board/page-window.ts and test only for attaching archive-specific list hydration and exporting its explicit revision. Existing page-end.ts/tests and URL-normalization fixes belong to previous Grok+root integration; preserve their bytes. No engine/registry/type/schema/UI/proxy/policy changes. Root owns this plan, ERP revision integration, tests and deployment.

Use existing BoardConfig.createListSession API for historical GTP only. At fetchBoardWindow entry derive an archive config: all other source configs remain unchanged; GTP creates a session using the provided guarded fetch function to read original list and, only for real policy rows with exact 마감 label, their official detail. Reuse original parseGtpList and isGtpDropTitle; keep normal source config/parser unchanged. Parse list with actual table.t01 tbody tr, six cells, td.subject a title and numeric fn_goView ID, period td.last. Policy closed rows missing identity must fail the page rather than disappear. Explicit non-policy rows stay excluded by the existing title rule.

Detail URL is canonical https://pms.gtp.or.kr/web/business/webBusinessView.do?b_idx=<numeric-id>. Official detail uses <dt>접수 기간</dt><dd>2026-08-07 00:00&nbsp;~&nbsp;2026-08-31 18:00</dd>. Only the adjacent dd of this exact whitespace-normalized label is evidence. Require exactly one unambiguous period and two valid calendar dates via existing normalizers/parseApplyPeriod, start<=end, and an ended period for a list-marked closed announcement. Missing/invalid/ambiguous/contradictory dates or any detail failure abort that archive page so cursor never advances or writes partial page records. No date inference, global regex over full body or today-as-deadline. Use existing guarded fetch for host allowlist, timeouts and abort; no raw fetch bypass.

Hydrate only the known period cell with the validated source dates and return HTML to the unchanged parser; do not change title, canonical sourceId, agency, attachments or body-filling rules. Fetch each distinct detail at most once per list page. Already dated rows need no extra request. Empty/end pages remain original HTML for existing source-end handling. Errors stay incomplete, never empty/end. The archive wrapper should preserve existing session behavior if relevant and keep state scoped to the current window.

Export an explicit source archive revision helper from page-window (for example archiveCollectionRevisionOf(cfg): string|null), returning a fixed GTP detail-date revision only for GTP and null for other sources. Root will add that field conditionally into ERP's existing source revision hash so only GTP restarts page1 to recover previously skipped rows. Existing stored rows and all other cursors remain intact; no manual counter/lock edits.

Regression requirements: normal GTP still omits closed rows; archived synthetic real-layout closed rows receive exact parsed source dates and keep IDs/title/agency;10closedrows produce10closed-date announcements; no unnecessary detail calls for dated/non-policy rows; duplicateID doesn't duplicate fetch; detail error/missing/ambiguous/invalid/future period yields incomplete same cursor and no partial result; abort through guarded fetch; wrapper does not affect other configs/end/normalization tests; GTP revision fixed and othersnull. Root actual10of10captured pages proof remains private and is used for live/captured-input verification.

Finish with focused tests/full shared tests/typecheck, qualified independent review including prior archive-page changes, exact pin+build+push+deploy-check for threeapps, ERP source-specific revision guard tests and real GTP cursor/date/store verification. Parent owns execution; worker no shell/network/modelAPI/commit/deploy/install. Preserve attribution and prior native-timeout status; do not claim the prior interrupted worker completed.
