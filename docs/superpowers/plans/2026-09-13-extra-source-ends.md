# Prove two additional historical board ends

Goal: recognize the confirmed last pages of PIPA and GJSinbo without treating undated policy rows, missing data, stale pages or an error as completion. Root owns design and integration. A different isolated worker is hardening page-end.ts and GTP; do not touch those files.

Create only src/collect/board/additional-page-end.ts and its test. Export a pure isProvenAdditionalSourceEnd(html, cfg, requestedPage) boolean. Root will call it from the existing page-end predicate after login/error rejection. No edits to other runtime files, source parsers, schema, dates, collector state, UI or normal collection. The existing two-consecutive-page rule remains in charge of final completion.

Observed PIPA page12 and13: ul.table contains a li.tr.thead header and only pinned li.tr rows identified by div.board_num.notice. An explicit direct child div states 등록된 게시물이 없습니다. Pagination .paginate contains fn_egov_link_page links; li.last points to11. Require the proper list and pagination containers, exact empty notice, a positive safe-integer requestedPage beyond the independently shown last page, no ordinary rows and every remaining row structurally identifiable as a pinned notice. Do not treat a dated or undated ordinary row as empty. Existing pinned IDs are collected on page1 and repeat after the list ends; their repetition alone is never end evidence. Validate expected link/call form and title for pinned rows; malformed rows are unknown.

Observed GJSinbo page11 and12: .bbs-list contains .r-count > strong total192, form[name=bbs_form] with hidden bbs_id=1 and post_view_mod=list, .bbs-basic-list > .rows containing whitespace only, and div.page with a first-page link and a previous-page link. Fixed source page size is20 (source contract and actual pages1/2). Last page is ceil(total/20)=10. The previous link is search_page=10 on requested11 and11 on requested12. Require a positive safe-integer total and requested page beyond its last page, the correct source/board query context (notification1 and bbs_id=1), exactly empty rows (no elements or non-whitespace text), and previous-page link matching requestedPage-1. Preserve normal list or blank/error unknown states. Missing count, malformed/cross-board links, stale previous page and a populated box must return false.

Tests use minimal observed fixtures for both consecutive requested pages. Cover source mismatch, malformed/noninteger/nonpositive page or count, missing/incorrect last/previous page, removed containers, non-pinned PIPA rows, malformed pinned rows, GJSinbo hidden links/elements, and ordinary text/footer/login without board evidence. Search forms are not errors. Root will run captured/current live response proof and integrate with shared login/error rejection.

Worker must not run shell/network/model APIs, install packages, commit, push or deploy. Root owns tests, integration, independent review and exact consumer deployment. Existing archive/GTP review findings are tracked in their separate task; do not fix them in this worktree.
