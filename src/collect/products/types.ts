/**
 * 껍데기 — 실물은 공용 꾸러미 `@wedly/policy-match-shared` 의 `src/funding/products/types.ts` 에 있다.
 * ERP·일루아가 **같은 커밋**을 물어 판정·화면이 글자 단위로 같아지게 한다(설계서 §3).
 * 이 파일에 구현을 다시 적지 마라 — 그 순간 두 앱이 도로 갈라진다.
 */
export * from "../../funding/products/types";
