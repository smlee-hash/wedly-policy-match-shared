/**
 * `src/ai/` — 공고 1건 정밀 판정(AI)의 **지시문·응답 형식**만 모아 둔 자리.
 *
 * 여기 있는 것은 전부 **순수 함수와 상수**다. 모델을 실제로 부르는 일(열쇠·예산 장부·스트림)은
 * 앱이 한다 — ERP 는 사내 열쇠와 사내 장부로, 랩(`wedly-policy-lab`)은 자기 열쇠와 자기 장부로.
 * 그래야 「전문가가 본 AI 판정」과 「직원이 본 AI 판정」이 **같은 지시문·같은 판본**에서 나온다.
 *
 * ★`VERDICT_VERSION` 은 저장해 둔 판정을 언제 버릴지 정하는 판본이다. 지시문을 고치면
 *  반드시 함께 올린다 — 안 올리면 옛 지시문으로 만든 판정이 새 지시문 결과인 척 남는다.
 */
export * from "./tacit-types";
export * from "./verdict";
export * from "./breakthrough";
